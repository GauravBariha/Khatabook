<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
<link
    href="https://cdn.jsdelivr.net/npm/remixicon@4.5.0/fonts/remixicon.css"
    rel="stylesheet"
/>
<body class="bg-zinc-100">

    <nav class="flex justify-between p-3">
        <h3  class="text-2xl tracking-tight cursor-pointer "><a href="/">Khatabook</a></h3>
        <div class="navlinks flex gap-5">
            <a class="tracking-tight bg-blue-400 rounded-md py-1 px-3 text-white" href="/">Home</a>
            <a class="tracking-tight bg-blue-400 rounded-md py-1 px-3 text-white" href="/create">Create New Hisaab</a>
        </div>
    </nav>

    <div class="px-10 hisaabs">
        <h3 class="capitalize text-2xl font-medium mb-5 mt-10 tracking-tight">All Hisaab Kitaab</h3>
               
       <% if(files.length > 0){ %>
            <% files.forEach((file) => { %>
                 <div class="hisaab w-fit items-center mt-2 py-3 px-5 gap-20 bg-blue-500 flex justify-between rounded">
                    <div class="flex gap-4 text-white items-center">
                            <h3 class="text-2xl"><%= file %></h3>
                            <a href="/hisaab/<%= file %>"><i class="ri-arrow-right-circle-line"></i></a>
                    </div>
                    <div class="flex gap-3 items-center">
                        <a href="/edit/<%= file %>"><i class="ri-edit-box-line "></i></a>
                        <a href="/delete/<%= file %>"><i class="ri-delete-bin-6-line"></i></a>
                    </div>
            </div>
            <% }) %>
       
        <% } else { %>
            <h1><i class="ri-emotion-sad-line flex justify-center items-center text-4xl"></i></h1>
            <h3 class="text-zinc-500 flex justify-center items-center">currently nothing to show, create hisaab to see something</h3> 
        <% } %> 
        
    </div>


</body>
</html>